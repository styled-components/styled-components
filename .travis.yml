language: node_js
dist: trusty
node_js:
  - 6
  - 8
script:
  - node --version
  - yarn --version
  - yarn test
  - yarn flow
  - yarn lint
  - yarn typescript
  - yarn tslint
notifications:
  email:
    on_failure: change
cache:
  yarn: true
  bundler: true
  directories:
    - node_modules

---
'styled-components': major
---

Mounting the same `createGlobalStyle` component multiple times emits its CSS only once. Rendering output stays byte-stable across SSR, client, and Server Components.

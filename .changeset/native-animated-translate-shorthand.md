---
'styled-components': patch
---

React Native: multi-argument transform shorthands (`translate(x, y)`, `translate3d(x, y, z)`, and `scale(x, y)`) animate correctly per axis inside `@keyframes` or a `transition`, alongside single-axis forms and uniform `scale(n)`. This applies on both iOS and Android and on react-native-web.
